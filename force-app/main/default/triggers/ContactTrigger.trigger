trigger ContactTrigger on Contact (before insert, after insert, before update, after update) {
    /*if (Trigger.isBefore) {
        if (Trigger.isUpdate) {
            system.debug('we are in Before update trigger');
            ContactTriggerHandler.updateContactValidation1(Trigger.New, Trigger.Old, Trigger.NewMap, Trigger.OldMap);
            ContactTriggerHandler.contactValidationUpdat2(trigger.new, trigger.old, trigger.oldMap, trigger.newMap);
        }
    }*/






}



    /*if(trigger.isBefore ){

    system.debug('we are in before trigger');

if(trigger.isInsert){


    system.debug('we are in before update trigger');
}
if(trigger.isUpdate){


    system.debug('we are in before update triger');
}
}
if(trigger.isAfter){
    system.debug('we are in after trigger');


if(trigger.isInsert){
    system.debug('we are in after insert trigger');
}
if(trigger.isUpdate){

    system.debug('we are in after update trigger');
}
}*/
